# คัดลอกเป็น .env.local แล้วใส่ค่าจริง

# URL ของ Nest backend ที่ Next เรียกจากฝั่งเซิร์ฟเวอร์
NEST_API_URL=http://localhost:3001

# URL ที่เบราว์เซอร์ใช้ต่อ WebSocket (ไม่ใช่ความลับ จึงใส่ NEXT_PUBLIC_ ได้)
NEXT_PUBLIC_WS_URL=http://localhost:3001
